{
  "airspec": "1.1",
  "meta": { "title": "Missing binding default" },
  "parameters": [
    {
      "id": "sortMode",
      "type": "select",
      "label": "Sort",
      "options": {
        "type": "static",
        "values": [{ "value": "name", "label": "Name" }]
      }
    }
  ],
  "datasets": [
    {
      "id": "ordersList",
      "source": "orders",
      "operation": "list",
      "fields": ["order_id"],
      "bindings": {
        "sort": {
          "parameter": "sortMode",
          "cases": [
            { "equals": "name", "value": [{ "field": "order_id", "direction": "ascending" }] }
          ]
        }
      }
    }
  ],
  "layout": { "id": "root", "type": "text", "text": "Invalid" }
}
